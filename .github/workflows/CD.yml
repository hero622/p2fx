name: CD

on:
  push:
    tags: '*'

jobs:
  build-lin:
    name: Linux Build
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with: { fetch-depth: 0 }
      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y g++-10-multilib
      - name: Build
        env:
          RELEASE_BUILD: 1
          DEMO_SIGN_PUBKEY: ${{ secrets.P2FX_DEMO_SIGN_PUBKEY }}
          DEMO_SIGN_PRIVKEY: ${{ secrets.P2FX_DEMO_SIGN_PRIVKEY }}
        run: make -j$(nproc)
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: p2fx-linux
          path: p2fx.so
          if-no-files-found: error
  build-win:
    name: Windows Build
    runs-on: windows-2019
    env:
      POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with: { fetch-depth: 0 }
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.1
      - name: Build
        env:
          RELEASE_BUILD: 1
          DEMO_SIGN_PUBKEY: ${{ secrets.P2FX_DEMO_SIGN_PUBKEY }}
          DEMO_SIGN_PRIVKEY: ${{ secrets.P2FX_DEMO_SIGN_PRIVKEY }}
        run: msbuild -m -t:p2fx -p:"Configuration=Release;Platform=x86" p2fx.sln
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: p2fx-windows
          path: bin\p2fx.dll
          if-no-files-found: error
      - name: Upload PDB Artifact
        uses: actions/upload-artifact@v3
        with:
          name: p2fx-windows-pdb
          path: bin\p2fx.pdb
          if-no-files-found: error
  release:
    name: Release
    if: github.repository == 'Zyntex1/p2fx'
    needs: [build-lin, build-win]
    runs-on: ubuntu-latest
    steps:
      - name: Get Release Version
        id: get_release
        run: echo ::set-output name=version::${GITHUB_REF:10}
      - name: Download Linux Build
        uses: actions/download-artifact@v3
        with:
          name: p2fx-linux
      - name: Download Windows Build
        uses: actions/download-artifact@v3
        with:
          name: p2fx-windows
      - name: Download Windows PDB
        uses: actions/download-artifact@v3
        with:
          name: p2fx-windows-pdb
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          body: |
            ![CD](https://github.com/Zyntex1/p2fx/workflows/CD/badge.svg)

            **Installation**
            - Download:
              - [Windows](https://github.com/Zyntex1/p2fx/releases/download/${{ steps.get_release.outputs.version }}/p2fx.dll)
              - [Linux](https://github.com/Zyntex1/p2fx/releases/download/${{ steps.get_release.outputs.version }}/p2fx.so)
            - Place the binary into the game folder e.g. `Portal 2`
            - Open developer console and enter `plugin_load p2fx`

            ---

            **Changelog**
            TODO
          files: |
            p2fx.so
            p2fx.dll
            p2fx.pdb
          prerelease: ${{ contains(github.ref, '-pre') }}
          fail_on_unmatched_files: true
