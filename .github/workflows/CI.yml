name: CI

on:
  push:
    branches: '**'
    tags-ignore: '**'
    paths-ignore:
      - '.github/*'
      - '.github/workflows/CD.yml'
      - '.mailmap'
      - '.gitattributes'
      - '.gitignore'
      - 'docs/**'
      - '**.md'
      - 'LICENSE'
  pull_request:
  workflow_dispatch:

jobs:
  build-win:
    name: Windows Build
    runs-on: windows-2019
    env:
      POWERSHELL_TELEMETRY_OPTOUT: 1
    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with: { fetch-depth: 0 }
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v1.1
      - name: Build
        run: msbuild -m -t:p2fx -p:"Configuration=Release;Platform=x86" p2fx.sln
      - name: Upload Artifact
        uses: actions/upload-artifact@v3
        with:
          name: p2fx-windows
          path: |
            bin\p2fx.dll
            bin\p2fx.pdb
          if-no-files-found: error
  blender-scripts:
    name: Blender Scripts
    runs-on: ubuntu-latest
    steps:
      - name: Zip File
        uses: montudor/action-zip@v1
        with:
          args: zip -qq -r p2fx-blender-scripts.zip . -i blender\p2fx
      - name: Upload Zip Artifact
        uses: actions/upload-artifact@v3
        with:
          name: p2fx-blender-scripts
          path: p2fx-blender-scripts.zip
          if-no-files-found: error